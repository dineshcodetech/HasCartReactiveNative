# API Configuration
# For Android physical device, use your computer's IP address
# For Android emulator, use 10.0.2.2
# For iOS simulator, use localhost

# Device IP for Android (physical device)
ANDROID_DEVICE_IP=192.168.0.25

# Device IP for iOS (simulator)
IOS_DEVICE_IP=localhost

# API Port
API_PORT=3000
